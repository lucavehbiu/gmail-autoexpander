{
  "name": "gmail-unlimited-backend",
  "version": "1.1.0",
  "description": "Stripe payment and license backend for Gmail Unlimited",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "migrate": "psql \"$DATABASE_URL\" -f db/schema.sql",
    "issue-license": "node scripts/issue-license.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "stripe": "^14.0.0",
    "micro": "^10.0.1",
    "micro-cors": "^0.1.1"
  }
}
